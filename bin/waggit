#!/usr/bin/env ruby

require 'waggit.rb'

for arg in ARGV
  puts "#{arg}"
end

cmd = ARGV[0].downcase

case

when (cmd == "s" or cmd == "sync")
  Waggit.sync
when (cmd == "f" or cmd == "forcepush" or cmd == "fp")
  Waggit.forcepush
else
  puts "Invalid command!"
end
